data_type: userAcceleration
trajectory_dim: 20
trajectory_len: 200

# model_cls: VectorFieldMLP
# model_params:
#   hidden_dim: 20
#   num_layers: 2

model_cls: VectorFieldLinear
model_params:
  in_features: 20
  out_features: 20

device: cuda
batch_size: 32
num_epochs: 30

test_ratio: 0.2